# DiffEnergy

Set up conda enviroment as following:
```
conda create -n diffenergy python=3.10
conda activate diffenergy
cd /path/to/repo/root
pip install .
```
To install in editable mode, use:
```
pip install -e .
```
